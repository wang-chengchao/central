spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: git@github.com:wangChengChao/conf-repo.git
          skip-ssl-validation: true
server:
  port: 8888
management:
  endpoints:
    web:
      exposure:
        include: '*'
